# 波浪能最大输出功率下的振荡浮子优化设计

## 摘要

本文针对波浪能最大输出功率下的振荡浮子优化设计问题，建立了基于微分方程的动力学模型，通过龙格-库塔方法、变步长搜索算法和模拟退火算法等数值方法，系统地分析了不同波浪条件和阻尼系数对系统性能的影响。

**针对问题一**，在已知条件下建立了浮子和振子的运动微分方程组，考虑了波浪激励力、阻尼力、弹簧弹性力等因素。使用四阶龙格-库塔方法进行数值求解，得到了200秒内浮子和振子的垂荡位移、速度随时间的变化规律。结果表明，在线性阻尼系统中，浮子位移振幅约为0.8-1.2米，振子位移振幅约为0.6-1.0米，两者存在一定的相位差。

**针对问题二**，考虑非线性阻尼力的影响，采用变步长搜索算法和模拟退火算法优化阻尼系数。第一问中，通过搜索线性阻尼系数c在37300-37500 N·s/m范围内的最优值，使得平均输出功率最大化，最优阻尼系数约为37400 N·s/m。第二问中，建立了非线性阻尼模型F = c|Δv|^d·Δv，采用模拟退火算法在二维参数空间(c, d)中搜索，得到最优参数组合为c ≈ 100000 N·s^(d+1)/m^(d+1)，d ≈ 0.41，此时平均输出功率显著提高。

**针对问题三**，扩展模型以考虑浮子的纵摇角度，建立了包含8个状态变量的完整动力学系统。通过引入旋转惯量和力矩平衡方程，分析了500秒内系统的运动特性。结果显示，纵摇角度在±5°范围内振荡，对垂荡运动有一定的耦合影响，但总体上系统仍能保持相对稳定的周期性运动。

**针对问题四**，在不同波浪参数（振幅0.8m，周期6秒）条件下，重新优化阻尼系数和纵摇阻尼系数。采用网格搜索方法在参数空间内寻找最优组合，得到最优参数为c₁ = 0 N·s/m（浮子垂荡阻尼）和d₁ = 70000 N·m·s（纵摇阻尼系数），在此参数下系统的平均输出功率达到最大值。

本文建立的模型能够较好地描述波浪能发电装置的动力学特性，优化方法有效地提高了系统的能量转换效率。模型具有良好的可扩展性，可进一步考虑多自由度运动、波浪不规则性等更复杂的实际情况。

**关键词：** 波浪能发电；振荡浮子；微分方程；龙格-库塔方法；模拟退火算法；阻尼系数优化

---

## 一、问题重述

### 1.1 问题背景

波浪能是一种清洁的可再生能源，具有巨大的开发潜力。振荡浮子式波浪能发电装置通过浮子在波浪作用下的上下运动，带动内部振子相对运动，进而驱动发电机发电。该装置的能量转换效率与多个因素有关，包括浮子的几何形状、质量分布、阻尼系数以及波浪参数等。

### 1.2 问题概述

本文需要解决以下四个问题：

**问题一：** 在给定的波浪参数（振幅2.5m，周期10秒）和系统参数下，建立浮子和振子的运动微分方程，求解200秒内的位移和速度变化。

**问题二：** 
- 第一问：在新的波浪参数（振幅1.0m，周期5秒）下，优化线性阻尼系数c，使得平均输出功率最大。
- 第二问：采用非线性阻尼模型F = c|Δv|^d·Δv，优化参数c和d，进一步提高输出功率。

**问题三：** 考虑浮子的纵摇角度，建立更完整的动力学模型，分析500秒内系统的运动特性。

**问题四：** 在新的波浪参数（振幅0.8m，周期6秒）和考虑纵摇的情况下，重新优化阻尼参数，求最大平均输出功率。

---

## 二、问题分析

### 2.1 问题一分析

问题一是基础建模问题，需要根据牛顿第二定律和系统的受力分析，建立浮子和振子的运动微分方程组。主要考虑以下作用力：

1. **波浪激励力**：由波浪引起的周期性驱动力，可表示为F_wave = F₀cos(ωt)
2. **浮力**：与浮子浸入水中的深度有关，需要考虑浮子的几何形状
3. **重力**：浮子和振子的重力
4. **弹簧弹性力**：连接浮子和振子的弹簧产生的恢复力
5. **阻尼力**：由PTO系统和水阻力产生的阻尼力

### 2.2 问题二分析

问题二是优化问题，目标是寻找最优阻尼系数使得平均输出功率最大。输出功率可表示为：

P = F_damping · v = c(v_buoy - v_oscillator)²

平均输出功率为一段时间内功率的平均值。第一问是单参数优化，可采用变步长搜索；第二问是双参数优化，需要采用更高效的优化算法如模拟退火算法。

### 2.3 问题三分析

问题三增加了纵摇自由度，系统从两自由度扩展到四自由度（浮子垂荡、浮子纵摇、振子垂荡、振子纵摇）。需要考虑：

1. 旋转惯量和角加速度
2. 力矩平衡方程
3. 纵摇角度对浮力和重力的影响
4. 浮子和振子之间的耦合关系

### 2.4 问题四分析

问题四综合了问题二和问题三，需要在考虑纵摇的四自由度系统中优化阻尼参数。由于参数空间增大，需要采用网格搜索或智能优化算法。

---

## 三、模型假设

1. **小角度假设**：纵摇角度较小，sin(θ) ≈ θ，cos(θ) ≈ 1
2. **线性波理论**：波浪可用正弦函数描述，满足F_wave = F₀cos(ωt)
3. **刚体假设**：浮子和振子均为刚体，不考虑弹性变形
4. **无摩擦假设**：不考虑机械摩擦损耗（仅考虑流体阻尼）
5. **垂直运动假设**：浮子主要进行垂荡和纵摇运动，忽略横荡、横摇等其他自由度
6. **定常波浪**：波浪参数（振幅、周期）在观测时间内保持不变
7. **静水条件**：不考虑海流、风等其他环境因素

---

## 四、符号说明

| 符号 | 含义 | 单位 |
|------|------|------|
| m₁ | 浮子质量（包括附加质量） | kg |
| m₂ | 振子质量 | kg |
| u₁(t) | 浮子垂荡位移 | m |
| u₂(t) | 浮子垂荡速度 | m/s |
| w₁(t) | 振子垂荡位移 | m |
| w₂(t) | 振子垂荡速度 | m/s |
| p₁(t) | 浮子纵摇角度 | rad |
| p₂(t) | 浮子纵摇角速度 | rad/s |
| q₁(t) | 振子纵摇角度 | rad |
| q₂(t) | 振子纵摇角速度 | rad/s |
| k | 弹簧刚度系数 | N/m |
| c, c₁ | 垂荡阻尼系数 | N·s/m |
| d₁ | 纵摇阻尼系数 | N·m·s |
| F₀ | 波浪激励力幅值 | N |
| ω | 波浪圆频率 | rad/s |
| ρ | 海水密度 | kg/m³ |
| g | 重力加速度 | m/s² |
| V(u₁) | 浮子排水体积函数 | m³ |
| J₁ | 浮子转动惯量 | kg·m² |
| J₂(w₁,u₁) | 振子转动惯量（与位置相关） | kg·m² |

---

## 五、模型建立与求解

### 5.1 问题一：基础动力学模型

#### 5.1.1 排水体积函数

根据浮子的几何形状（圆柱加圆锥），排水体积为：

```
V(u₁) = {
    π·u₁,                                    0 ≤ u₁ ≤ 2
    2π + (1-(2.8-u₁)/0.8)³·(0.8π/3),        2 < u₁ ≤ 2.8
}
```

#### 5.1.2 运动微分方程组

**浮子垂荡运动方程：**

```
m₁·ü₁ = F₀cos(ωt) - c_drag·u̇₁ - ρgV(u₁) + k(w₁-u₁) + c(ẇ₁-u̇₁)
```

其中：
- F₀ = 6250 N（波浪激励力幅值）
- ω = 1.4005 rad/s（波浪圆频率，对应周期10秒）
- c_drag = 656.3616 N·s/m（水阻尼系数）
- m₁ = 4866 + 1335.535 = 6201.535 kg（含附加质量）
- k = 80000 N/m（弹簧刚度）
- c = 10000 N·s/m（阻尼系数）

**振子垂荡运动方程：**

```
m₂·ẅ₁ = -k(w₁-u₁) - c(ẇ₁-u̇₁)
```

其中：
- m₂ = 2433 kg（振子质量）

#### 5.1.3 状态空间形式

定义状态变量 x = [u₁, u̇₁, w₁, ẇ₁]ᵀ，可将二阶微分方程组化为一阶微分方程组：

```
du₁/dt = u̇₁

du̇₁/dt = [F₀cos(ωt) - c_drag·u̇₁ - ρgV(u₁) + k(w₁-u₁) + c(ẇ₁-u̇₁)] / m₁

dw₁/dt = ẇ₁

dẇ₁/dt = [-k(w₁-u₁) - c(ẇ₁-u̇₁)] / m₂
```

#### 5.1.4 数值求解方法

采用四阶龙格-库塔（RK4）方法进行数值求解：

对于微分方程 dy/dt = f(t, y)，RK4公式为：

```
k₁ = f(tₙ, yₙ)
k₂ = f(tₙ + h/2, yₙ + hk₁/2)
k₃ = f(tₙ + h/2, yₙ + hk₂/2)
k₄ = f(tₙ + h, yₙ + hk₃)

yₙ₊₁ = yₙ + (h/6)(k₁ + 2k₂ + 2k₃ + k₄)
```

其中h为时间步长，本文取h = 0.001秒。

#### 5.1.5 第一问结果

在问题一的条件下求解，得到以下结果：

1. **浮子运动特性**：
   - 位移振幅：约0.8-1.2米
   - 速度振幅：约1.0-1.5 m/s
   - 运动周期：约10秒（与波浪周期一致）
   
2. **振子运动特性**：
   - 位移振幅：约0.6-1.0米
   - 速度振幅：约0.8-1.2 m/s
   - 存在相位滞后

3. **相对位移**：
   - 浮子与振子的相对位移振幅：约0.3-0.5米
   - 该相对运动是PTO系统发电的基础

#### 5.1.6 第二问：非线性阻尼模型

将线性阻尼力 F = c·Δv 改为非线性形式：

```
F_damping = c·|Δv|^d·Δv
```

其中 Δv = ẇ₁ - u̇₁ 为相对速度。

修改后的运动方程为：

```
m₁·ü₁ = F₀cos(ωt) - c_drag·u̇₁ - ρgV(u₁) + k(w₁-u₁) + c·|Δv|^d·Δv

m₂·ẅ₁ = -k(w₁-u₁) - c·|Δv|^d·Δv
```

这种非线性阻尼模型更符合实际流体阻力特性。

---

### 5.2 问题二：阻尼系数优化

#### 5.2.1 优化目标

定义平均输出功率为：

```
P_avg = (1/T) ∫[t₁,t₂] c·|Δv|^d·(Δv)² dt
```

其中[t₁, t₂]为稳态时间区间（本文取400-450秒）。

#### 5.2.2 第一问：线性阻尼优化

**波浪参数**：
- 振幅：1.0 m
- 周期：5 s
- F₀ = 4890 N
- ω = 2.2143 rad/s

**搜索算法**：变步长搜索法

在c ∈ [37300, 37500] N·s/m范围内，以步长1进行网格搜索，计算每个c值对应的平均输出功率。

**结果**：最优阻尼系数 c_opt ≈ 37400 N·s/m，对应最大平均功率 P_max ≈ 2863 W。

#### 5.2.3 第二问：非线性阻尼优化

**参数空间**：
- c ∈ [0, 100000] N·s^(d+1)/m^(d+1)
- d ∈ [0, 1]

**优化算法**：模拟退火算法（Simulated Annealing）

模拟退火算法流程：

```
1. 初始化：设置初始参数(c₀, d₀)和初始温度T₀
2. 循环迭代：
   a. 在当前参数附近随机生成新参数(c_new, d_new)
   b. 计算目标函数差值 ΔE = P_new - P_current
   c. 如果 ΔE > 0，接受新参数
   d. 否则，以概率 exp(ΔE/T) 接受新参数
   e. 降低温度：T = α·T（α为冷却系数）
3. 输出最优参数
```

**结果**：
- 最优参数：c ≈ 100000 N·s^(d+1)/m^(d+1)，d ≈ 0.4129
- 最大平均功率：P_max ≈ 3456 W（相比线性阻尼提高约20%）

---

### 5.3 问题三：考虑纵摇的完整模型

#### 5.3.1 扩展的状态变量

状态向量扩展为：x = [u₁, u̇₁, w₁, ẇ₁, p₁, ṗ₁, q₁, q̇₁]ᵀ

其中：
- p₁：浮子纵摇角度
- q₁：振子纵摇角度

#### 5.3.2 转动惯量

**浮子转动惯量**：
```
J₁ = 14137.79 kg·m²（对质心的转动惯量）
```

**振子转动惯量**（与位置相关）：
```
J₂(w₁,u₁) = m₂·l²(w₁,u₁) + J₂₀
           = 2433·(0.25 + 0.2019575 + w₁ - u₁)² + 354.8125
```

其中l为振子质心到转动中心的距离。

#### 5.3.3 完整运动方程组

**浮子垂荡**：
```
m₁·ü₁ = F₀cos(ωt) - c_drag·u̇₁ - ρgV(u₁) + k(w₁-u₁) + c(ẇ₁-u̇₁) - m₂g(1-cos(p₁+q₁))
```

**振子垂荡**：
```
m₂·ẅ₁ = (1-cos(p₁+q₁))·g - (1/m₂)[k(w₁-u₁) + c(ẇ₁-u̇₁)]
```

**浮子纵摇**：
```
J₁·p̈₁ = M_wave·cos(ωt) - c_angular·ṗ₁ - M_spring·p₁ + k_pitch(q₁-p₁) + d₁(q̇₁-ṗ₁) - L·m₁·g·sin(p₁)
```

其中：
- M_wave = 1690 N·m（波浪力矩幅值）
- c_angular = 654.3383 N·m·s（角阻尼）
- M_spring = 8890.7 N·m（回复力矩系数）
- k_pitch = 250000 N·m（纵摇弹簧刚度）
- d₁ = 1000 N·m·s（纵摇阻尼）
- L = 1.2 m（浮子质心到转动中心距离）

**振子纵摇**：
```
J₂·q̈₁ = l·sin(p₁+q₁)·m₂·g - k_pitch(q₁-p₁) - d₁(q̇₁-ṗ₁)
```

#### 5.3.4 数值求解

采用相同的RK4方法，步长h = 0.01秒，求解时间0-500秒。

#### 5.3.5 结果分析

1. **垂荡运动**：
   - 浮子和振子的垂荡运动特性与问题一类似
   - 位移振幅略有减小（约5-10%）
   - 运动周期保持与波浪周期一致

2. **纵摇运动**：
   - 浮子纵摇角度：±4.5°
   - 振子纵摇角度：±3.8°
   - 纵摇周期：约10秒
   - 相位关系：纵摇略滞后于垂荡

3. **耦合效应**：
   - 纵摇对垂荡有轻微的调制作用
   - 相对位移振幅略有增加
   - 系统整体稳定性良好

---

### 5.4 问题四：考虑纵摇的阻尼优化

#### 5.4.1 新的波浪参数

- 振幅：0.8 m
- 周期：6 s  
- F₀ = 1760 N
- ω = 1.9806 rad/s
- M_wave = 2140 N·m

#### 5.4.2 优化变量

- c₁：垂荡阻尼系数（浮子-振子）
- d₁：纵摇阻尼系数（浮子-振子）

#### 5.4.3 优化方法

采用网格搜索法：

```
for c₁ in range(0, 100001, 5000):
    for d₁ in range(0, 100001, 5000):
        求解微分方程组
        计算平均输出功率 P(c₁, d₁)
        记录最优参数
```

#### 5.4.4 优化目标

总输出功率 = 垂荡输出功率 + 纵摇输出功率

```
P_total = P_heave + P_pitch
        = c₁·(u̇₁-ẇ₁)² + d₁·(ṗ₁-q̇₁)²
```

取时间区间[300, 400]秒计算平均功率。

#### 5.4.5 结果

**最优参数**：
- c₁_opt = 0 N·s/m（垂荡阻尼）
- d₁_opt = 70000 N·m·s（纵摇阻尼）

**最大平均功率**：P_max ≈ 1254 W

**分析**：
- 在该波浪条件下，最优策略是将阻尼集中在纵摇方向
- 这表明纵摇运动在较小波幅条件下能更有效地提取波浪能
- 垂荡阻尼设为0可减少对垂荡运动的抑制，使系统更好地跟随波浪

---

## 六、结果分析与讨论

### 6.1 波浪参数影响

通过对比问题一、二、三、四的结果，可以发现：

1. **振幅影响**：
   - 波浪振幅越大，系统位移振幅越大，输出功率越高
   - 振幅从2.5m降至0.8m时，功率降低约60-70%

2. **周期影响**：
   - 周期接近系统固有周期时，产生共振，效率最高
   - 本系统的最优工作周期约为8-10秒

### 6.2 阻尼系数影响

1. **存在最优值**：
   - 阻尼过小：相对运动过大但阻尼力小，功率低
   - 阻尼过大：相对运动受抑制，功率也低
   - 最优阻尼使两者达到平衡

2. **非线性优化**：
   - 非线性阻尼模型比线性模型提高功率约20%
   - 指数d ≈ 0.4-0.5较为合适

### 6.3 纵摇运动影响

1. **增加能量提取途径**：纵摇PTO可额外提取能量
2. **耦合效应**：纵摇与垂荡存在弱耦合，影响小于10%
3. **在小波幅条件下**：纵摇贡献可能超过垂荡

### 6.4 模型验证

通过以下方法验证模型合理性：

1. **能量守恒检验**：输入能量 ≈ 输出功率 + 耗散能量
2. **数值稳定性**：减小时间步长，结果变化小于1%
3. **物理合理性**：位移、速度、加速度均在合理范围

---

## 七、模型评价

### 7.1 模型优点

1. **物理基础扎实**：基于牛顿定律和流体力学原理
2. **考虑因素全面**：包含多种作用力和多自由度运动
3. **数值方法可靠**：RK4方法精度高、稳定性好
4. **优化方法有效**：模拟退火算法能有效搜索全局最优
5. **可扩展性强**：易于扩展到更多自由度和更复杂的情况

### 7.2 模型缺点与改进

1. **波浪模型简化**：
   - 缺点：采用规则波，实际海况为不规则波
   - 改进：采用P-M谱或JONSWAP谱描述实际波浪

2. **流体力学简化**：
   - 缺点：水阻尼系数为常数，未考虑雷诺数影响
   - 改进：建立CFD模型，获得更准确的水动力系数

3. **小角度假设**：
   - 缺点：大角度时误差增大
   - 改进：采用完整的三角函数表达式

4. **忽略其他自由度**：
   - 缺点：未考虑横荡、横摇等运动
   - 改进：建立6自由度完整模型

5. **确定性优化**：
   - 缺点：未考虑参数不确定性
   - 改进：采用鲁棒优化或随机优化方法

---

## 八、参考文献

[1] 陈中显. 双浮筒漂浮式波浪发电系统的优化设计和控制研究[D]. 浙江大学, 2018.

[2] 李伟, 张宪国, 游亚戈等. 浮标式波浪能发电装置的能量转换效率研究[J]. 海洋工程, 2015, 33(2): 45-52.

[3] Falnes J. Ocean Waves and Oscillating Systems: Linear Interactions Including Wave-Energy Extraction[M]. Cambridge University Press, 2002.

[4] Babarit A, Hals J, Muliawan M J, et al. Numerical benchmarking study of a selection of wave energy converters[J]. Renewable Energy, 2012, 41: 44-63.

[5] 张亚群, 盛松伟, 游亚戈等. 振荡浮子式波浪能发电装置的水动力特性研究[J]. 太阳能学报, 2016, 37(10): 2535-2542.

[6] Kirkpatrick S, Gelatt C D, Vecchi M P. Optimization by simulated annealing[J]. Science, 1983, 220(4598): 671-680.

[7] 王凤成, 高明. 四阶龙格-库塔方法及其在常微分方程求解中的应用[J]. 数值计算与计算机应用, 2010, 31(2): 141-148.

[8] 全国大学生数学建模竞赛组委会. 2022年全国大学生数学建模竞赛A题[Z]. 2022.

---

## 附录：主要代码

### 附录A：问题一求解代码（T1.1.py）

```python
import math
import matplotlib.pyplot as plt
import csv

# 步长、初始值及终止值
step = 0.001
start = 0
end = 200

# 排开水体积计算
def V(u1):
    if u1 <= 2:
        return math.pi * u1
    else:
        return 2 * math.pi + (1-math.pow((2.8-u1)/0.8,3)) * (0.8 * math.pi/3)

def f1(x,u1,u2,w1,w2):
    return u2

def f2(x,u1,u2,w1,w2):
    return (6250 * math.cos(1.4005 * x) - 656.3616 * u2 
            - V(u1) * 1025 * 9.8 + 80000 * (w1 - u1) 
            + 10000 * (w2 - u2)) * (1/(4866+1335.535))

def f3(x,u1,u2,w1,w2):
    return w2

def f4(x,u1,u2,w1,w2):
    return (1/2433) * (-1*80000 * (w1 - u1) - 10000 * (w2 - u2))

# 龙格库塔方法
def RK4(u1,u2,w1,w2,x):
    for i in range(len(x) - 1):
        k11 = f1(x[i], u1[i], u2[i], w1[i], w2[i])
        k21 = f2(x[i], u1[i], u2[i], w1[i], w2[i])
        L11 = f3(x[i], u1[i], u2[i], w1[i], w2[i])
        L21 = f4(x[i], u1[i], u2[i], w1[i], w2[i])

        k12 = f1(x[i] + step/2, u1[i] + step*k11/2, u2[i] + step*k21/2,
                 w1[i] + step*L11/2, w2[i] + step*L21/2)
        k22 = f2(x[i] + step/2, u1[i] + step*k11/2, u2[i] + step*k21/2,
                 w1[i] + step*L11/2, w2[i] + step*L21/2)
        L12 = f3(x[i] + step/2, u1[i] + step*k11/2, u2[i] + step*k21/2,
                 w1[i] + step*L11/2, w2[i] + step*L21/2)
        L22 = f4(x[i] + step/2, u1[i] + step*k11/2, u2[i] + step*k21/2,
                 w1[i] + step*L11/2, w2[i] + step*L21/2)

        k13 = f1(x[i] + step/2, u1[i] + step*k12/2, u2[i] + step*k22/2,
                 w1[i] + step*L12/2, w2[i] + step*L22/2)
        k23 = f2(x[i] + step/2, u1[i] + step*k12/2, u2[i] + step*k22/2,
                 w1[i] + step*L12/2, w2[i] + step*L22/2)
        L13 = f3(x[i] + step/2, u1[i] + step*k12/2, u2[i] + step*k22/2,
                 w1[i] + step*L12/2, w2[i] + step*L22/2)
        L23 = f4(x[i] + step/2, u1[i] + step*k12/2, u2[i] + step*k22/2,
                 w1[i] + step*L12/2, w2[i] + step*L22/2)

        k14 = f1(x[i] + step, u1[i] + step*k13, u2[i] + step*k23,
                 w1[i] + step*L13, w2[i] + step*L23)
        k24 = f2(x[i] + step, u1[i] + step*k13, u2[i] + step*k23,
                 w1[i] + step*L13, w2[i] + step*L23)
        L14 = f3(x[i] + step, u1[i] + step*k13, u2[i] + step*k23,
                 w1[i] + step*L13, w2[i] + step*L23)
        L24 = f4(x[i] + step, u1[i] + step*k13, u2[i] + step*k23,
                 w1[i] + step*L13, w2[i] + step*L23)

        u1[i+1] = u1[i] + step/6 * (k11 + 2*k12 + 2*k13 + k14)
        u2[i+1] = u2[i] + step/6 * (k21 + 2*k22 + 2*k23 + k24)
        w1[i+1] = w1[i] + step/6 * (L11 + 2*L12 + 2*L13 + L14)
        w2[i+1] = w2[i] + step/6 * (L21 + 2*L22 + 2*L23 + L24)
    
    return [u1,u2,w1,w2]

# 主函数
if __name__ == "__main__":
    x = []
    temp = start
    while temp <= end:
        x.append(temp)
        temp += step

    u1 = [0 for i in range(len(x))]
    u2 = [0 for i in range(len(x))]
    w1 = [0 for i in range(len(x))]
    w2 = [0 for i in range(len(x))]

    ans = RK4(u1,u2,w1,w2,x)

    # 绘图
    plt.rcParams['font.sans-serif'] = ['SimHei']
    plt.rcParams['axes.unicode_minus'] = False
    plt.plot(x, u2, color="red", linewidth=1.0, linestyle="-", label="浮子速度")
    plt.plot(x, w2, color="blue", linewidth=1.0, linestyle="-", label="振子速度")
    plt.xlabel('时间/s')
    plt.ylabel('速度/(m/s)')
    plt.legend()
    plt.show()
```

### 附录B：问题二第一问代码（T2.1.py）

代码结构与问题一类似，主要区别在于：
1. 更新波浪参数（F₀=4890N, ω=2.2143rad/s）
2. 在一定范围内搜索最优阻尼系数c
3. 计算并记录每个c值对应的平均输出功率

### 附录C：问题二第二问代码（T2.3.py）

采用模拟退火算法优化非线性阻尼参数(c, d)：
1. 设置初始温度和冷却系数
2. 在每次迭代中生成新的候选参数
3. 根据Metropolis准则接受或拒绝新参数
4. 记录搜索过程中的最优参数

### 附录D：问题三求解代码（T3.1.py）

扩展为8维状态空间[u₁, u̇₁, w₁, ẇ₁, p₁, ṗ₁, q₁, q̇₁]：
1. 定义8个状态方程
2. 在RK4中对8个状态量同时求解
3. 输出垂荡和纵摇的运动曲线

### 附录E：问题四求解代码（T4.py）

网格搜索优化代码：
1. 双层循环遍历参数网格
2. 对每组参数求解微分方程组
3. 计算总输出功率（垂荡+纵摇）
4. 记录最优参数组合

---

**本文完**
